#

## 备注
- Node/Edge  id 均不存重复

## Syntax

常规
```
{
    name： '', 
    syntax: '', // 当前值，默认空。
    expressions: {
        <syntax>: '' // syntax 值，为字符串
    }
}
```

单syntax
```
{
    name： '', 
    syntax: '',  // keep empty
    expressions: {
        'Literal': '' // 固定为 Literal
   }
}
```
